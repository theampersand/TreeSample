BinarySearchTree pseudocode

add newNode to key
if newNode > key
{
	if key.right == null
		key.right = newNode
	else
		add newNode to key.right
}
if newNode < key
{
	if key.left == null
		key.left = newNode
	else
		add newNode to key.left
}
value already in tree